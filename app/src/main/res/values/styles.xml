<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Debug Panel items -->
    <style name="DebugItemText" parent="Theme.Coolometer">
        <item name="android:textSize">18sp</item>
        <item name="android:ellipsize">end</item>
        <item name="android:gravity">start</item>
        <item name="maxLines">2</item>
    </style>

    <!-- History Table items -->
    <style name="HistoryTabItem" parent="TextAppearance.AppCompat">
        <item name="android:textSize">18sp</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">4dp</item>
    </style>

    <!-- Alert Dialogs -->
    <style name="Cool.MaterialAlertDialog.Title.Text" parent="MaterialAlertDialog.Material3.Title.Text">
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Cool.MaterialAlertDialog.Button" parent="Widget.Material3.Button.TextButton.Dialog">
        <item name="android:textColor">@color/secondaryDarkColor</item>
    </style>

</resources>